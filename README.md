# Figma макет
https://www.figma.com/design/KfP8foLiV3T94WMcWo9eet/To-Do-List--Community-?node-id=0-1&t=aqpuRp9c85hTLiRZ-1

---

# 📝 Todo App  
Простое и удобное веб-приложение для управления списком задач. Работает полностью в браузере, задачи сохраняются в `localStorage`, а интерфейс выполнен в современном минималистичном стиле.

---

## 🚀 Возможности

- Добавление новых задач  
- Удаление задач  
- Отметка задачи как выполненной  
- Редактирование по двойному клику  
- Фильтры: **All**, **Active**, **Completed**  
- Автосохранение задач в браузере  
- Простой и чистый UI  

---

## 🛠 Технологии

- **HTML** — структура приложения  
- **CSS** — оформление интерфейса  
- **JavaScript** — логика управления задачами  
- **localStorage** — сохранение задач между перезагрузками  

---

## 📦 Установка и запуск

1. Скачайте или клонируйте проект  
   ```bash
   git clone https://github.com/your-name/todo-app.git

2. Перейдите в папку проекта

   ```bash
   cd todo-app
   ```

3. Откройте `index.html` в браузере

   * двойным кликом
   * или через локальный сервер:

   ```bash
   npx serve .
   ```

   или

   ```bash
   python -m http.server 5500
   ```

---

## 📂 Структура проекта

```
todo-app/
│── index.html
│── styles.css
│── script.js
└── README.md
```

---

## 🧠 Как это работает

Приложение хранит все задачи в `localStorage`:

```js
localStorage.setItem("taskdo.tasks.v1", JSON.stringify(tasks));
```

События и логика:

* `addEventListener()` — обработка кликов и нажатий клавиш
* DOM-манипуляции — создание, обновление и удаление элементов
* Рендеринг списка задач через шаблон
* Редактирование задачи через `contenteditable`
* Подсчёт активных задач и отображение фильтров

---

## 📜 Лицензия

Свободно используйте, изменяйте и улучшайте.
